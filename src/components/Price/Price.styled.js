import styled from 'styled-components';
import Container from 'react-bootstrap/Container';

export const PriceWrapper = styled.section`
  margin: 2rem 0;
`;

export const PriceItemsContainer = styled(Container)`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
`;
